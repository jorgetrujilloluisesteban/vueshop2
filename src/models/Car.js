class Car {
    constructor(carro, productoid, cantidad, fecha) {
        this.carro = carro;
        this.productoid = productoid;
        this.cantidad = cantidad;
        this.fecha = fecha;
    }
}

export default Car;